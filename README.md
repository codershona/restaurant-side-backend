# Learning Nodejs



```
   PROJECT SETUP:

   * npm init;
   * Add ("start": "node index");
   * Run npm start ;
   * Installing Express framework: npm install express@4.16.3 --save;
   *  Morgan is used for logging purposes: npm install morgan@1.9.0 --save;
   * Setting up a REST API: npm install body-parser@1.18.3 --save;
   * Express restful api;
   * Install express-generator : sudo npm install express-generator@4.16.0 -g;
   * Scaffolding an Express Application: express conFusionServer;
   * Next, move to the conFusionServer folder. cd conFusionServer/;
   * Type the following at the command prompt to install all the Node modules: npm install ;
   * Run npm start ;
   * Start the MongoDB server: mongod --dbpath=data --bind_ip 127.0.0.1 ;
   * mongo ;
   * MongoDB serve Shell:   db
     use conFusion
     db
     db.help()

    # Install the Node MongoDB driver and the Assert:

   * npm install mongodb@3.0.10 --save;
   * npm install assert@1.4.1 --save;
   * REST API with Express, MongoDB and Mongoose: npm install mongoose@5.1.7 mongoose-currency@0.2.0 --save;
   * Using cookie-parser: npm install cookie-parser@1.4.3 --save'
   * Installing express session and session file : npm install express-session@1.15.6 session-file-store@1.2.0 --save;

   * Installing Passport: npm install passport@0.4.0 passport-local@1.0.0 passport-local-mongoose@5.0.1 --save;

   * Assignment

```